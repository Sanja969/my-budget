<div class="d-flex justify-content-between align-items-center m-0 p-3">
  <div id="menuToggle">
    <input type="checkbox" />
      <span></span>
      <span></span>
      <span></span>
  <ul id="menu">

    <li><%= link_to "Home", groups_path %><br /></li>
    <li>
      <% if user_signed_in? %>
        <%= button_to "Logout", destroy_user_session_path, form: {'data-turbo' => "false"}, method: :delete, class:"btn btn-none"  %>
      <% else %>
        <%= link_to "Register", auth_path %><br />
      <% end %>
    </li>
  </ul>
  </div>
  <h4 class="text-center text-white p-0"><%= title %></h4>
  <% if other=="⇦"%>
    <%= link_to "#{other}", url_for(:back), class:"text-white h2"%>
  <% else %>
    <button class="search-header"><%= other %></button>
  <% end %>
</div>
